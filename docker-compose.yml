services:
  yad2bot:
    build: .
    container_name: yad2bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/yad2bot.db
    volumes:
      - yad2bot_data:/app/data

volumes:
  yad2bot_data:
